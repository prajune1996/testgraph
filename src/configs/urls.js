const PREFIX = {
  CURRENCY: "https://open.er-api.com/v6",
};

/**
 * URLS
 */
export const API_URLS = {
  /**
   * Auth Url's
   */
  CURRENCY: {
    FETCH_RATES: PREFIX.CURRENCY + "/latest",
  },
};
